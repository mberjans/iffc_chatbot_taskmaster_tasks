"""
NER backend implementations for different NER systems.

This package contains implementations for different NER backends, such as spaCy,
scispaCy, etc. Each backend must implement the NERBackend interface.
"""
